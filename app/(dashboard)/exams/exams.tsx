"use client";
import React from "react";
import styled from "styled-components";

const exams = () => {
  return <PageWrapper>This is the exams page</PageWrapper>;
};

export default exams;

const PageWrapper = styled.div``;
